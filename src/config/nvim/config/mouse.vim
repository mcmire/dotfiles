" Mouse settings
"===============

" Mouse support is turned on by default in Neovim. When using Vim on a laptop
" one can swipe the trackpad accidentally, which would cause Vim to start
" scrolling. This is annoying, so turn this off.
set mouse=
