" Configuration for markdown-preview.nvim
" =======================================

" Don't close the browser tab housing the preview for the current Markdown file
" when switching to a different buffer
let g:mkdp_auto_close = 0
